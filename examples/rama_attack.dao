
load graphics;

var resource = Graphics::Resource()

var scene = resource.LoadDaeFile( "../media/rama_attack/rama_attack.dae" )
scene.SetBackground( 0.5, 0.5, 0.5, 0.5 )

var window = Graphics::Window( 960, 800, '' )
window.Show( scene, 500, 1 )
window.Quit()
